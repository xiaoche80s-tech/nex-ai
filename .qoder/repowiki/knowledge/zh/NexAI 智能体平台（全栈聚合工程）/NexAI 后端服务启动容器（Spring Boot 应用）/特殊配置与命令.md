构建并运行需先 `mvn package` 生成 `target/nexai-server.jar`，再执行 `java -jar nexai-server.jar`；Docker 镜像构建命令为 `docker build -t nexai-server .`，启动时可通过 `-e JAVA_OPTS=...` 和 `-e ARGS=...` 覆盖 JVM 参数与 Spring Boot 命令行参数；本地开发需在 `application-local.yaml` 中开启 devtools 热部署。