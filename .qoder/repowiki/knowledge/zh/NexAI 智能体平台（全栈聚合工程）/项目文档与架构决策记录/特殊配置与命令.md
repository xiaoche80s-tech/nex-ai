M1 验收需启动 nexai-server（local profile, 48080）、远端 PostgreSQL（菜单 6400+ 与 ai_* 表已初始化、租户 1）、前端 pnpm dev（5173），并通过 `mvn test -pl nexai-module-ai` 运行 147 例回归、`script/smoke/m1-acceptance.sh` 执行冒烟（支持 mock 与 real 凭据模式）。