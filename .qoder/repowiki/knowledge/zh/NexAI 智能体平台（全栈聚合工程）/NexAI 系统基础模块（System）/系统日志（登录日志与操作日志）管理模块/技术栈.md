Spring Boot + Spring Security (`@PreAuthorize`) 实现权限控制；Swagger v3 (`@Operation`、`@Tag`) 生成接口文档；Apache POI/ExcelUtils 实现 Excel 导出；dromara-trans (`@TransMethodResult`) 实现字段国际化翻译；框架通用组件 `CommonResult`、`PageResult`、`BeanUtils` 统一返回与对象转换。