Spring Boot + Spring Security（`@PreAuthorize` 权限控制）、Swagger/OpenAPI（`@Tag`/`@Operation`）、Spring Cache（`@Cacheable`/`@CacheEvict` 配合 RedisKeyConstants 缓存短信模板）、Hutool（正则与工具类）、Lombok、MyBatis Mapper 访问数据库。