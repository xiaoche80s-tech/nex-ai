新增一种存储后端时，需在 `FileStorageEnum` 中登记 storage 编号、对应的 `*ClientConfig` 与 `*Client` 类，并在对应子包下实现 `doInit()` 及 upload/delete/getContent/presign* 方法；运行时通过 `/infra/file-config/update` 更新配置后，`FileClientFactoryImpl.refresh` 会检测配置变化并重新初始化客户端。