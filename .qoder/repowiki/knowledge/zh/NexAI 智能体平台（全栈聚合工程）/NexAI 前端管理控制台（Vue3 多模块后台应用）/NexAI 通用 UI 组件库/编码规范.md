- 每个组件目录提供 index.ts 作为对外入口，src 目录下放置 .vue 实现文件
- 基于 Element Plus 的组件统一通过 props/v-model 暴露双向绑定能力
- 复杂编辑器（DiyEditor、FormCreate、bpmnProcessDesigner、SimpleProcessDesignerV2）采用 config.ts + property.vue 的元数据驱动配置模式
- 可复用的选择器类组件（DeptSelectForm、UserSelectForm、AppLinkInput）以独立表单形式封装并支持 v-model