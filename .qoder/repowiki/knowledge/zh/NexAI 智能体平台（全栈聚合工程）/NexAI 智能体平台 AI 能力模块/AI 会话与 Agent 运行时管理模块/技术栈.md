Spring Web MVC + Reactor（Flux/Sink）实现 SSE 流式事件；agentscope HarnessAgent + ChatCompletionsStreamingAdapter 作为智能体运行时；PostgresAgentStateStore 按 (userId,sessionId) 槽位持久化会话状态；Jackson 序列化 RuntimeEvent JSON 载荷。