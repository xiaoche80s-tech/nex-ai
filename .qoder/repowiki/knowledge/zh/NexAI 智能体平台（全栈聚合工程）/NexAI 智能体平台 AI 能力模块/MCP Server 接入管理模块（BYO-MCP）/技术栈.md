Spring Boot + Spring Security（`@PreAuthorize` 权限）、MyBatis（Mapper + EncryptTypeHandler AES 加密列）、Reactor（`Mono.defer` 延迟链式调用）、agentscope `McpClientWrapper` 直连 MCP Server（stdio/SSE/Streamable HTTP），探测整体超时上限 20 秒。