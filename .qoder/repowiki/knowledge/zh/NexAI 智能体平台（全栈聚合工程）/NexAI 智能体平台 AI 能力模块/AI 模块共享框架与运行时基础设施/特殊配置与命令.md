OpenTelemetry 追踪默认关闭，需设置 `nexai.ai.observability.otel.enabled=true` 并通过 `otel.*` 标准属性（如 `otel.traces.exporter=otlp`、`otel.exporter.otlp.traces.endpoint`）配置导出器与采样；workspace/skills 根目录默认位于 `~/.agentscope/nexai/workspace` 与 `~/.agentscope/nexai/skills`，支持 `~/` 展开为用户 home 目录。