Spring Boot `@ConfigurationProperties` + `@Component` 扫描；OpenTelemetry SDK autoconfigure（`AutoConfiguredOpenTelemetrySdk`）经 `otel.*` 标准属性驱动；agentscope `@Tool`/`@ToolParam` 注解用于工具方法注册；Spring Security `AuthorizeHttpRequestsCustomizer` 与 `FilterRegistrationBean` 装配 OpenAI 兼容出口鉴权。