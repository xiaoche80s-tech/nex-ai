- 通过静态内部 Application 类配合 @Import 显式引入所需自动配置类，避免全量扫描带来的额外开销。
- 所有 SpringBootTest 基类统一启用 @ActiveProfiles("unit-test") 并设置 WebEnvironment.NONE，隔离业务环境。
- 测试基类之间采用继承组合关系：BaseDbAndRedisUnitTest 复用 BaseDbUnitTest 的能力并叠加 Redis 配置。
- 对外暴露的测试能力以可继承的抽象基类形式提供，而非注解或 API，便于子类直接 extends 使用。