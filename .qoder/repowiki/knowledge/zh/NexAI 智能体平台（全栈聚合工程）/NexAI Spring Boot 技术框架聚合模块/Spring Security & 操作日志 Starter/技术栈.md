Spring Security + Spring Boot 3 AutoConfiguration；密码加密使用 BCrypt；Token 校验通过 `OAuth2TokenCommonApi` 远程调用；操作日志集成 mzt-logapi（`@EnableLogRecord` + `ILogRecordService`）；上下文传递使用 `TransmittableThreadLocal` 风格策略。