Spring Boot AutoConfiguration + AOPalliance MethodInterceptor；SQL 重写基于 MyBatis-Plus 的 `DataPermissionInterceptor` 与 JSqlParser（`net.sf.jsqlparser`）表达式树；可选依赖 `nexai-spring-boot-starter-security` 以获取 `LoginUser` 与权限 API。