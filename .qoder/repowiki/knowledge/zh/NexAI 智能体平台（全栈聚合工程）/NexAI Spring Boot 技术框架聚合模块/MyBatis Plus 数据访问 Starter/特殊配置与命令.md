通过 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 实现 Spring Boot 3.2+ 自动装配；`@AutoConfiguration(before = MybatisPlusAutoConfiguration.class)` 确保 MapperScan 先于 MyBatis Plus 初始化以避免扫描警告；`IdTypeEnvironmentPostProcessor` 在环境阶段解析 DbType 以决定主键生成策略。