Java + Lombok + MapStruct；JSON 使用 `tools.jackson`（Jackson 3.x 兼容层）并通过 Hutool JSON 辅助判断；HTTP/Spring 相关依赖均标记 `provided`；集成 OpenTelemetry API 用于链路追踪工具；使用 `com.alibaba:transmittable-thread-local` 传递线程上下文；通过 `cn.hutool:hutool-all` 提供集合/字符串等基础能力。