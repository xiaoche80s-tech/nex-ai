Spring Boot AutoConfiguration + Spring Data Redis（RedisTemplate、RedisMessageListenerContainer、StreamMessageListenerContainer）+ Redisson 分布式锁；RabbitMQ 通过 `spring-amqp` 的 `JacksonJsonMessageConverter` 可选接入；JSON 序列化统一使用框架内 `JsonUtils`。