启用 Redis Stream 消费需确保 Redis 版本 ≥ 5.0.0（启动时在 `checkRedisVersion` 中校验），且需开启 Spring Scheduling（`@EnableScheduling` 已在 Consumer 配置类中启用）；RabbitMQ 支持仅在 classpath 存在 `org.springframework.amqp.rabbit.core.RabbitTemplate` 时生效。