基于 `org.lionsoul:ip2region` 的 XDB 离线检索实现 IP→地区编号映射；使用 Hutool (`cn.hutool.core`) 的 `ResourceUtil` 与 `CsvUtil` 加载 resources 下的 `ip2region.xdb` 与 `area.csv`；Jackson 注解处理 `Area` 树的双向 JSON 引用；Lombok 生成构造器与 getter/setter。