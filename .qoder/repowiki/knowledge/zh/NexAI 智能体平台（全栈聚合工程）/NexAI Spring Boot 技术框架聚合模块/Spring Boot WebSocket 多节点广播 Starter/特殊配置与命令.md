通过 `application.yml` 中 `nexai.websocket.enable` 开关启用/禁用整个 WebSocket 功能；通过 `nexai.websocket.sender-type` 选择 local/redis/kafka/rabbitmq/rocketmq 五种广播后端，不同后端还需配置对应 topic/exchange 等属性（如 `nexai.websocket.sender-kafka.topic`）。